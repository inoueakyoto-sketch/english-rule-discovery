# RULE FINDER｜英語のしくみ発見 v0.1.3 adaptive practice candidate

中学生が英文法の規則を「説明される前に少し違う文へ出会う → 自分なりに分類する → 新しい規則を知る」の順で発見し、その後は短い練習で定着させる、スマホ優先の静的Webアプリです。

## v0.1.3 の3本柱

ホームから次の3つを選べます。

1. **発見をつづける**
   - SV → SVC → SVO → SVOO → SVOC → M の順に、未知を1つずつ投入
   - 未発見の役割名は先に表示しない
   - 正誤にかかわらず、未知の形へ出会った後で解説する
2. **5問だけ練習する**
   - 発見済みルールだけを出題
   - 本人にはランダムに見えるが、内部では「最近間違えた・まだ練習回数が少ない・しばらく出ていない」規則を優先
   - 同じ規則が続きすぎないよう重みを調整
   - 5問で必ず一区切りにし、結果を表示
3. **気づきを振り返る**
   - 発見済みルールを自分の気づきとして保存
   - 問題中も1タップで戻れる
   - 練習で間違えたときは「気づきを見て、同じ問題をもう一度」が可能

## 定着データ

各ルールごとにブラウザ内へ以下を保存します。

- 練習回数
- 正解回数 / 誤答回数
- 最後に練習した時刻
- 最後の結果

これらから練習の出題重みを決めます。画面では細かい正答率を前面に出さず、

- これから
- 練習中
- あと少し
- かなり分かってきた
- もう一度

のような言葉で「今の手ごたえ」を表示します。

## 学習フロー

発見モード：

SV → 未習SVC → C解説 → SVO → O解説 → SVOO → SVOC → SVC+M → M解説

各現在文型で3問の安定正解を目安に次の未知パターンを投入します。

練習モード：

発見済みルール → 適応ランダム5問 → 必要なら気づきノート → 同じ問題へ再挑戦 → 5問のまとめ

## UI/UX

- スマホ390px前後を主基準
- 中学生が持ちやすい、少し大人っぽいダークネイビー＋ブルーのプロダクト調UI
- 親指操作を意識した下部操作ドック
- RULE MAPは発見済みだけ名称を表示
- 新規ルールのときだけ特別な発見演出
- 対応端末では控えめな振動フィードバック
- iPhoneセーフエリア対応
- `prefers-reduced-motion` 対応

## GitHub Pages

1. ZIPを展開します。
2. `index.html` がリポジトリの公開ルートに来るよう全ファイルを配置します。
3. VS Codeから `git add .` → `git commit` → `git push` します。
4. GitHub の Settings → Pages で `main` / `(root)` を公開元にします。

ビルド工程やサーバー処理は不要です。

## テスト

```bash
node tests/test-core.js
node tests/test-flow.js
node tests/test-practice.js
node tests/test-ui-contract.js
```

- 6学期 × 6パターン × 200文 = 7,200文の構造検証
- 発見前後の役割公開制御
- M初回がSVC＋Mになること
- 苦手優先の練習重み
- 習得ラベル判定
- app.js が参照するHTML IDの整合

## 現在の制約

語彙データは共通基礎語彙の試作です。今後、教科書・学年・学期・Unitごとの語彙データに差し替える前提です。
