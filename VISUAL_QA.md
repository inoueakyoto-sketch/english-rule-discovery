# Visual QA v1.0.0

**Visual Source of Truth:** `design/VISUAL_SOURCE_OF_TRUTH.png`

**Actual render set:** `qa/screens/`

**Contact sheet:** `qa/VISUAL_QA_CONTACT_SHEET.png`

## Gate

A visual release passes only when the real 390×844 render preserves the source-of-truth hierarchy, spacing logic, typography contrast, course-card language, discovery emphasis, and mobile touch layout. “Same mood” alone is not sufficient.

v1.0.0: **PASS**.

The live design deliberately hides unrevealed grammar roles/patterns even when the concept mockup displays them, because answer leakage conflicts with the app’s discovery-learning rule.
